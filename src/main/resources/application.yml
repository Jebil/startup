server:  port: 8080  compression:    enabled: truespring:
  datasource:
      url: ${DB_URL:jdbc:postgresql://127.0.0.1:5432/startup-system}
      username: ${DB_USER:pg-system-user}
      password: ${DB_PASSWORD:pgpassword}      